# Skill: /delivery — PRODUCTION_DELIVERY_LOOP

`/delivery` is sufficient by itself. No extra delegation language is required.

Source-of-truth for this repo:

1. `.claude/commands/delivery.md` — self-contained command with project adapter
2. `AGENTS.md` → Agent Command Registry → `/delivery`
3. `docs/delivery-managed-agent-plan.md` — managed agent upgrade path

Default live target: `https://myalchemy.vercel.app/`

SUCCESS requires live proof on this URL unless another target is explicitly requested.
