# Agent Memory Index

Always load `active.md`.

Use topic files only when relevant:

- `topics/delivery.md` — delivery workflow, verification, PR/deploy behavior
- `topics/audit.md` — audit workflow and issue-writing behavior
- `topics/learn-pass.md` — auto-memory, candidate lessons, and self-learning triggers
- `topics/mobile.md` — mobile layout, viewport, navigation, touch/keyboard behavior
- `topics/ux.md` — product UX decisions and interaction rules
- `topics/copy.md` — client-facing wording and style rules

Use component notes only when editing that component.

Use candidates, metrics, archive, mistakes, harness proposals, and harness regression tests only during `/learn-pass`, `/memory-review`, or `/upgrade`.

Do not load the full memory tree by default. Classify the task scope first, then load the smallest useful set.
